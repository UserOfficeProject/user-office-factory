# NODE_ENV=development
# NODE_ENV=production
# DATABASE_HOSTNAME=localhost
# DATABASE_PORT=5432
# DATABASE_USER=duouser
# DATABASE_PASSWORD=duopassword
# DATABASE_DATABASE=duo

# PDF Generation Settings
# Maximum number of concurrent PDF generations (default: 20)
# Increase this if you have more CPU/memory, decrease if experiencing OOM or timeouts
# MAX_CONCURRENT_PDF_GENERATIONS=20

# Timeout for PDF generation in milliseconds (default: 60000 = 60 seconds)
# Increase this for complex PDFs or slow systems
# PDF_GENERATION_TIMEOUT=60000

# Enable HTML debug output for PDF generation
# PDF_DEBUG_HTML=1