{{#> layout}}

  <div class="container-fluid">
    <div class="row">
      <div class="col">
        <span class="title">{{step.topic.title}}</span><br />
        <br />

        {{#each step.fields}}
          {{> questionaryAnswer step=this attachments=../attachments}}
        {{/each}}

      </div>
    </div>
  </div>

{{/layout}}